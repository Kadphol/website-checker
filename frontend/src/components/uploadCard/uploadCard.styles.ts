import CSS from 'csstype';

export const iconUploadStyle: CSS.Properties = {
  maxWidth: '50px',
  minWidth: '30px',
  height: 'auto',
}

export const uploadCardStyle: CSS.Properties = {
  boxShadow: '0 0 5px #ccc',
  borderRadius: '15px'
}